# Node / Expo build artifacts
node_modules/
.expo/
.expo-shared/
web-build/
.expo-dev/
.expo/dev/

# Firebase and environment — never commit real secrets. .env.example is a
# template with no real values, so it's intentionally NOT excluded.
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
expo-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.iml
